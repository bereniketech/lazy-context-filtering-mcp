## [2026-04-01] ESLint scanned virtualenv files | What broke: `npm run lint` failed with 19 errors from `.venv/Lib/site-packages` JavaScript files | Root cause: ESLint was configured to lint the whole workspace without excluding Python virtualenv artifacts | Fix: added ignore rules for `.venv/**`, Python files, and `__pycache__` in flat ESLint config | File(s): eslint.config.js
## [2026-04-01] TypeScript rootDir blocked server tests | What broke: `npm run build` and `npm run typecheck` failed because `tests/server/test_mcp.ts` was outside configured `rootDir` | Root cause: `tsconfig.json` set `rootDir` to `src` while including `tests/**/*.ts` | Fix: removed `rootDir` so source and test TypeScript files can be type-checked together | File(s): tsconfig.json
## [2026-04-01] Vitest did not discover task-named test file | What broke: `npm run test` reported no tests found even after adding `tests/server/test_mcp.ts` | Root cause: default Vitest include pattern only discovers `*.test.ts` and `*.spec.ts` test entry files | Fix: added `tests/server/test_mcp.test.ts` as loader and updated test script to execute that path directly | File(s): tests/server/test_mcp.test.ts, package.json
## [2026-04-01] package.json parse error after script edit | What broke: `npm run build` and `npm run typecheck` failed with `EJSONPARSE` | Root cause: missing trailing comma after the `scripts` object while editing test script | Fix: restored valid JSON structure and re-ran verification pipeline | File(s): package.json
## [2026-04-01] registerContext params mismatch | What broke: new register tool tests crashed with `Cannot read properties of undefined (reading 'content')` | Root cause: implementation expected `(deps, input)` but call sites/tests used a single params object | Fix: changed `registerContext` to accept one object `{ store, engineClient, idGenerator?, input }` and updated MCP call site | File(s): src/server/tools/register.ts, src/server/index.ts
## [2026-04-01] list tool metadata cleanup lint failure | What broke: `npm run lint` failed with `@typescript-eslint/no-unused-vars` for an intentionally ignored `summary` destructure variable | Root cause: helper used destructuring with an unused binding (`_ignoredSummary`) and lint rules still flag that binding | Fix: replaced destructuring with object clone + `delete rest.summary` so metadata cleanup remains explicit and lint-safe | File(s): src/server/tools/list.ts
